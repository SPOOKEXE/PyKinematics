# def MapValue(value, start, stop, newStart, newStop):
# 	return ((value - start) / (stop - start)) * (newStop - newStart) + newStart

# def ClampValue(value, minn, maxx):
# 	return max( min( value, maxx ), minn )